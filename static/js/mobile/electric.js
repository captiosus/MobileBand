var electric = new Electric(true);
// electric.instrument.volume.value = -20;
// var tuna = new Tuna(Tone.context);
//
// var overdrive = new tuna.Overdrive({
//     outputGain: 0.5,         //0 to 1+
//     drive: 0.7,              //0 to 1
//     curveAmount: 1,          //0 to 1
//     algorithmIndex: 0,       //0 to 5, selects one of our drive algorithms
//     bypass: 0
// });

// overdrive.connect(electric.instrument.context.destination);

var mobile = new Mobile(electric);
